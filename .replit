# Replit Configuration
modules = ["python-3.11", "uv"]

# Command to run your FastAPI application
run = "uvicorn app.main:app --host 0.0.0.0 --port 8080"

# Expose the port for Replit preview pane
[ports]
[ports.8080]
localPort = 8080
externalPort = 80
